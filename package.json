{
  "name": "stocksense",
  "version": "1.0.0",
  "description": "StockSense — Restaurant Inventory OS",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "mkdir -p dist && cp -r index.html styles.css app.js closing-stock-import.js dist/ 2>/dev/null || true"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
